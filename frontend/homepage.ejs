<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Homepage</title>
    <style>
       body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }
        .container {
            text-align: center;
        }
        h1 {
            font-size: 36px;
            color: #110a0a;
            margin-bottom: 20px;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.3);
        }
        form {
            display: inline-block;
            margin: 10px;
        }
        button {
            padding: 10px 20px;
            font-size: 16px;
            border: none;
            background-color: #FFA500;
            color: #fff;
            cursor: pointer;
            border-radius: 5px;
            box-shadow: 2px 2px 4px rgba(0, 0, 0, 0.3);
            transition: background-color 0.3s;
        }
        button:hover {
            background-color: #FFB732;
        }
        .logo {
            margin-bottom: 20px;
            width: 200px; /* Adjust the width as needed */
            height: auto; /* Maintain aspect ratio */
        }
    </style>
</head>
<body>
    <div class="container">
        <img class="logo" src="./logo.png" alt="Institute Logo">
        <h1>Welcome to MNNIT's own Quiz Platform</h1>
        <form action="/register_student_b" method="POST">
            <button type="submit">Register as Student</button>
        </form>
        <form action="/login_student_b" method="POST">
            <button type="submit">Login as Student</button>
        </form>
        <form action="/register_faculty_b" method="POST">
            <button type="submit">Register as Faculty</button>
        </form>
        <form action="/login_faculty_b" method="POST">
            <button type="submit">Login as Faculty</button>
        </form>
    </div>
</body>
</html>
